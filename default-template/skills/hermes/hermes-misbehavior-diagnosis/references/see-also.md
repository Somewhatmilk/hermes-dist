## See also

- `session` — companion umbrella for the host/infra side (docker, llama-swap, gateways, processes). Run that one first when the question is "is X up".
- `security` — credential architecture and the refusal of "agent holds the secret" patterns.
- `session` — Mnemosyne and session-DB backup/restore, used when state has been lost or corrupted.
