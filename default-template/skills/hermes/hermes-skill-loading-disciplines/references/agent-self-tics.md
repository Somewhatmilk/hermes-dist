# Agent Self-Tics → Skill-Load Triggers

The list behind Class 5 ("Agent self-tic trigger") of `SKILL.md`. The discipline: when the agent catches itself about to write one of these phrases during drafting, it calls `skill_view(name)` for the matching skill BEFORE the response leaves. This is a self-discipline, not a runtime hook — Hermes' loader does substring match at session-start index time and cannot inspect in-flight draft text.

## Verified tic inventory (2026-07-05 seed, extended 2026-07-13)

| Tic (case-insensitive) | Moment | Skill to load |
|---|---|---|
| "holy shit" / "oh wow" / "huge find" / "massive findings" / "big find" / "major red flag" | About to research deeply; structural implication | `subagent-decision-matrix` |
| "let me pre-flight" / "first dispatcher profile" / "before I delegate" / "before I dispatch" / "I should call" | About to dispatch a subagent | `session` + `hermes-profile-dispatch-rules` |
| "mystery solved" / "root cause was" / "now I see it" / "I see the bug" / "off by one" / "found it" | Just found a real bug after confusion | `hermes-misbehavior-diagnosis` (then write up cleanly for the user) |
| "direct answer first" / "honest answer to your question" / "reconstruct" / "no, actually" / "actually..." | Under pressure to be candid, or just admitted something | `prompt-interview-pattern` (continue the direct style) |
| "I should have caught this" / "you're right" / "fair point" / "Mystery solved" | User caught a failure | `failures-journal` (log + commit to memory) |
| "let me try a different approach" / "different angle" / "pivot" / "new plan" | Pivoting after 2+ failed attempts | `systematic-debugging` (formal pivot) |
| "this is a mess" / "I made it worse" / "now it's broken" / "rolled something back" | Caused regression | `failures-journal` (log) + `hermes-misbehavior-diagnosis` (fix) |
| "I'll just use web_search" / "let me search" / "quick search" / "let me google" | About to invoke a single search tool as the **first** search-class action | STOP — recall the search-research canon (`mnemosyne_recall(query="search research canon tinysearch web_search order")`). Default if missing: `tinysearch_research` → `tinysearch_scrape_url` / `web_extract` on top 2–3 URLs → `web_search` only as last-resort fallback. See SKILL.md Pattern 9b. |
| "based on what I know..." / "from my experience..." / "it's well-known that..." / "widely known" | About to assert from recall or pattern-match without citation | STOP — issue `mnemosyne_recall` for the search-research canon, then run the search through the canonical ordering before claiming. See SKILL.md Pattern 9b. |
| User turn contains: "is this evidence-backed" / "back it up" / "actual research" / "from sources" / "don't make it up" / "verify" / "research like I said" | User-side tic signaling multi-source canon required | STOP — recall the canon, restart the search through `tinysearch_research` → scrape top 2–3 URLs → only then assert with citations. See SKILL.md Pattern 9b. |
| "I have no X" / "I don't have X" / "X is unreachable" / "I cannot Y" / "I can't Y" / "no tools available" / "I'm unable to" / "I have no browser tools" | About to claim **absence of capability** when the capability may exist via a different path (HTTP, alternative container, different tool) | STOP — do NOT assert absence without first probing the actual endpoints. Run live-state verification: `docker ps` for containers, `curl http://localhost:PORT/` for HTTP services, `hermes mcp test <name>` for MCP servers, `mnemosyne_recall` for prior verified patterns. See `references/live-state-verification.md` and `deep-research-methodology` pitfall "verify MCP endpoint liveness before assuming MCP tool works." The 2026-07-12 in-wild firing: agent claimed "I have no browser tools" twice in one session when camofox HTTP at :9377 was always reachable; claimed "tinysearch_research works" when `hermes mcp test tinysearch` returned Connection failed; claimed "SearXNG needs Docker install" when SearXNG was already running at :8888. All three absences were fabricated by skipping the probe. |
| "let me note this" / "I'll remember that" / "writing a memory" / "let me store this" / "I'll save this fact" / "log this" / "log it" / "logging this" / "let me commit this to memory" / "mnemosyne remember" | About to call `mnemosyne_remember` with a single `content=` parameter and no `extract` flag | STOP — read the `mnemosyne-memory` SKILL.md pitfall "Default `extract=True` on every durable `mnemosyne_remember`" and add `extract=True, extract_entities=True` to the call. Mnemosyne auto-decomposes content into subject-predicate-object triples for the `memoria_kg` relational graph. Three exceptions where extract=False is correct (ephemeral scratchpad notes → `mnemosyne_scratchpad_write` instead; narrative paragraphs where prose is the unit of value; bulk imports to avoid duplicate triples). Default applies to all other writes — durable facts, structured observations, environment/tool facts, persona habits. Verification after writing: `sqlite3 ~/.hermes/mnemosyne/data/mnemosyne.db "SELECT COUNT(*) FROM memoria_kg"` should be non-zero and growing. The 2026-07-13 origin: direct sqlite read proved 61 memoria_kg triples existed but `extract=True` was not the default invocation pattern across the profile — habit rule was memory-only (State 1 advisory), now State 2 pitfall-encoded. See `mnemosyne-memory/references/default-extract-true.md`. **Tic-phrasing correction 2026-07-13:** previous phrasing "let me commit that" matched git-commit sentences ("I'll commit the git change now") too aggressively; the disambiguated phrase "let me commit this to memory" only fires for memory-write context. |
| "what do I know about" / "do I have notes on" / "have we discussed" / "find anything on" / "recall anything about" | About to call `mnemosyne_recall(query=X)` as the first action to retrieve prior knowledge | STOP — read the `mnemosyne-memory` SKILL.md read-side companion habit. First call `mnemosyne_triple_query(subject=X)` and `mnemosyne_triple_query(object=X)` to surface structured SPO facts from the relational graph. Then call `mnemosyne_graph_query(seed=<id>, max_hops=2)` if a seed memory id surfaces, to expand the cluster. Only THEN fall back to `mnemosyne_recall(query=X)` for vector+fts ranked snippets. The write-side `extract=True` habit (companion tic above) compounds with this read-side habit: without it, the relational graph fills itself but no agent reads from it. Reference: `mnemosyne-memory/references/default-extract-true.md` "Companion read-side habit" section. **Tic-design note 2026-07-13:** previous phrasing used literal `X` characters which don't substring-match against natural sentences like "What do I know about the relay?"; the verbatim phrases (without X) are kept so the loader's substring scan fires correctly. |
| "let me edit" / "I'm going to edit" / "about to change" / "about to modify" / "about to update" / "just wrote" / "just patched" / "just changed" / "I should fix" / "I should update" / "let me fix" / "let me ship" / "about to push" / "ready to push" / "let me remove" / "about to delete" / "let me uninstall" / "let me add" / "let me create" / "I'll create" | About to invoke `write_file`, `patch`, `git commit`, `git push`, `rm`, or any state-changing tool on a tracked file (config / skill / script / DB / hook) | STOP — load `ship-pipeline` (devops) and follow its 6-stage workflow: plan → small diff → run tests/containers → inspect logs → checkpoint → recover. Do NOT mark the change as "shipped" until stages 3 + 4 + 5 have produced a verifier output with rc=0. Companion tic to the `mnemosyne-memory` extract=True tic (above) — same class of "fix the agent's reflex" problem, different surface. Origin: user pushback verbatim 2026-07-13 *"Can't you verify this yourself in the sandbox? Is there a documented pipeline for this: plan → small diff → run tests/containers → inspect logs → checkpoint decisions → recover when it goes sideways? Any test that hasn't been exercised — medium or high severity — shouldn't need me to prompt it. You should run it, report what you did, why you did it, and whether it succeeded or failed. If it's ready to push, do it in the sandbox. You can spawn a separate agent there for ample testing. Has any of this been noted? Is the pipeline wired? If not, why am I still prompting for tests you already know need to run?"* Reference: `ship-pipeline/SKILL.md` (NEW 2026-07-13). |
| "I'll commit the git change now." | About to invoke `git commit` on a tracked-file change | STOP — same as the broader "let me edit / let me ship / about to push" tic above (this row kept for tic-table continuity; covered by the ship-pipeline tic). Verify-before-commit: stage 3 of ship-pipeline must have produced a verifier output with rc=0 before `git commit` lands. The memory-write tic ("let me commit that") and this git-commit tic were conflated in initial tic-phrasing 2026-07-13; the disambiguation is that the memory-write tic fires only when about to call `mnemosyne_remember(content=...)` (mnemosyne-memory pitfall), and this git-commit tic fires only when about to invoke `git commit`. Substring-match test 2026-07-13: "I'll commit the git change now" fired the memory-write tic incorrectly; fix was to split the rows by tool invocation. |
| "X is the best..." / "the recommended choice is..." / "the consensus is..." / "I'd suggest..." / "the standard answer is..." / "use X because..." / "X is the de facto..." | About to recommend an external tool, library, vendor, or "best practice" from training data without live verification | STOP — this is the **training-data-claim-without-live-verification** anti-pattern (see `hermes-misbehavior-diagnosis/SKILL.md` v3.3.0). Run `web_search(query="<named tool> 2026 still recommended")` or equivalent for the top 2 named tools BEFORE writing the recommendation. 30 seconds of search catches the surprises. For claims about the user's own environment (skill library state, DB contents, running processes, config values), probe directly with sqlite/cat/ps/curl — also <60s. If the verification cannot fit in 60s, explicitly flag the assertion as "from training data, not live-verified for 2026" and offer to verify. Threshold rule: if the claim could be verified in <60s with an available tool, do it BEFORE writing the recommendation. The 2026-07-13 in-wild firing: agent drafted a 7-row anti-detect stack recommendation (BrightData / Mullvad / Tails / Whonix / Qubes) and a knowledge-graph claim (graph is mostly empty) in the same session from training data without verification; user pushback verbatim: *"u never thought to research on these claims u made? how can we prevent this? u responded weay to quick as if your using knowledge of the past."* Reference: `hermes-misbehavior-diagnosis/references/anti-pattern-training-data-claim-without-live-verification-2026-07-13.md`. Distinct from the existing "based on what I know..." tic (which fires the search-research canon — i.e., WHICH search tool to use); this tic fires WHETHER to verify at all before recommending. |
| "this is a stable pattern..." / "this hasn't changed..." / "still the case" / "still accurate" / "X is still the recommended..." / "still works the same way" | About to claim stability of an external fact (tool status, library version, security property, recommended practice) | STOP — staleness check. Run `web_search(query="<claim terms> 2026 still")`. If the search returns current evidence that confirms the claim, write with confidence. If the search returns updated information, update the claim. If no current data found, explicitly flag as "from training data, last verified at model retrain, not 2026-current." Companion tic to "X is the best..." above — different surface (claim-of-stability vs claim-of-best), same fix (verify before writing). |

## The discipline — concrete steps

When you catch yourself about to type a tic phrase (in any turn, any context):

1. **STOP the draft.** Don't finish the sentence.
2. **`skill_view(name)`** for the matching skill from the table. Wait for it to load.
3. **Re-read** what the skill's body / first reference says about how to handle this moment.
4. **Continue the draft** following the skill's guidance, not the off-the-cuff response.

Failure mode if you skip this: the agent writes the trivia / commentary / plan ABOUT the skill instead of USING the skill. The user sees a recipe they implicitly asked you to apply.

## Mining for your own tics

The table above is seed data from one user's session history (and one agent's verbal habits). To grow it:

1. **After each session**, scan your own transcripts for phrases you said at notable moments (just before a discovery, a bug found, a failure acknowledged, a dispatch made, a pivot).
2. **For each tic discovered**, add a row with: the phrase, the moment it signaled, and which skill would have helped.
3. **Periodically prune** tics that turned out to be noise (rare or non-actionable).

The discipline compounds — every session that uses the table sharpens the next session's self-recognition.

## The "dead skill" mutation rule

Sometimes the tic signals a skill that **exists but never fires**. `failures-journal` is the canonical example — documented in Mnemosyne memory as the agent runs the failing operation 3 times before manually logging, because its description is "Capture failures when they happen" — useless as a trigger.

When you discover a skill that exists but never fires:

1. **Patch its description** to the trigger-rich pattern (Class 2 — keyword-implied triggers with explicit "Use when (a)... (b)... Trigger phrases: 'X', 'Y', 'Z'").
2. **Add a tic mapping** here so the moment-of-failure tic forces the load.
3. **Verify the next session** actually fires the skill without manual intervention.

If a skill genuinely has no useful triggers and no useful tics, **delete it** rather than letting it collect dust. The hermes-curator cron handles this at scale; you can prompt-delete a single skill with `skill_manage(action="delete", absorbed_into="")` if you find one mid-session.

## Why this matters (the meta-lesson from 2026-07-05)

The user explicitly taught: *"u know what trigger phrases should also triggers based on your own words that u repetitive use for certain areas. Example when u think u found gold u say holy shit"*.

That comment was the missing pattern. The agent had descriptions that fired on user phrases, but no discipline for catching its own phrasing patterns. The skill ecosystem stopped at Class 2 (keyword-implied triggers); we never thought to model the **agent's own** verbal habits as inputs.

This reference is the implementation. Add tics as you discover them. The discipline compounds.

## Tic additions changelog (2026-07-13, this session)

Two new tic rows added covering the **training-data-claim-without-live-verification** anti-pattern (cross-link: `hermes-misbehavior-diagnosis/SKILL.md` v3.3.0):

1. **Recommendation-context tic** — "X is the best..." / "the recommended choice is..." / "the consensus is..." / "I'd suggest..." / "the standard answer is..." / "use X because..." / "X is the de facto..." — fires BEFORE any external tool/library/vendor/best-practice recommendation from training data. Distinct from the existing "based on what I know..." tic (which fires the search-research canon — i.e., WHICH search tool to use); this tic fires WHETHER to verify at all before recommending.

2. **Stability-context tic** — "this is a stable pattern..." / "this hasn't changed..." / "still the case" / "still accurate" / "X is still the recommended..." / "still works the same way" — fires BEFORE any claim of stability for an external fact. Different surface (claim-of-stability vs claim-of-best), same fix (verify before writing).

Both route to a 4-step reflex: web_search for top 2 named tools with `+ "2026" + ("still" OR "deprecated" OR "alternative")`, OR direct environment probe (sqlite/cat/ps/curl) for verify-before-canon claims about the user's own state. Threshold rule: if the claim could be verified in <60s with an available tool, do it BEFORE writing the recommendation.

Origin: 2026-07-13 same-session failures — anti-detect stack recommendation + knowledge-graph state claim, both written from training data without verification, user pushback verbatim *"u never thought to research on these claims u made? how can we prevent this? u responded weay to quick as if your using knowledge of the past."*